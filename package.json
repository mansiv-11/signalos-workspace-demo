{
  "name": "failure-first-builder",
  "version": "1.0.0",
  "private": true,
  "description": "A challenge project that stress-tests product ideas before they are built.",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "check": "/opt/homebrew/opt/node@22/bin/node --check src/app.js"
  }
}
